/*var mongoose = require('mongoose');
var Schema = mongoose.Schema;

// set up a mongoose model
module.exports = mongoose.model('provaRastrelliera', new Schema({ 
	id: String,
    coordinate: Object,
        latitude: Double,
        longitude: Double,
}));*/